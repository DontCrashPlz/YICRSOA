<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:orientation="vertical" >

    <Button
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:layout_gravity="center_horizontal"
        android:id="@+id/layout_approval_btn_no_complete"
        android:text="待办事项"
        android:background="@drawable/server_complete_btn_background_selector"
        android:textColor="@android:color/white"
        android:layout_marginTop="16dp"
        android:drawablePadding="16dp"
        android:drawableLeft="@mipmap/red_dot"
        android:paddingRight="56dp"
        android:paddingLeft="42dp"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:layout_gravity="center_horizontal"
        android:id="@+id/layout_approval_btn_complete"
        android:text="已办事项"
        android:background="@drawable/server_complete_btn_background_selector"
        android:textColor="@android:color/white"
        android:layout_marginTop="8dp"
        android:drawablePadding="16dp"
        android:drawableLeft="@mipmap/green_dot"
        android:paddingRight="56dp"
        android:paddingLeft="42dp"/>

</LinearLayout>